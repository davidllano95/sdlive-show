# SD.Live — handoff after PR #137

**Date:** 2026-08-25 — America/Bogota  
**Runtime baseline:** `main` after PR #137 = `44c852f6b9d4f11aaa31bb779b2339e7eefbd735`  
**Status at handoff:** PR #137 merged + CI green; production Finance smoke pending.  
**Active gate:** Admin visual audit / stabilization.

## Source precedence

`current main + verified production → current schema/config → this handoff → PROJECT_STATUS → README → historical master roadmap`.

## Change workflow

`inspect current main → short branch → implement → tests/CI → PR → CI green → squash merge → one production smoke for runtime changes`.

During visual audit, collect findings first and batch-fix after the coherent block. Only blocking P0/P1/runtime regressions should interrupt that method.

## Architecture that must not drift

- GitHub `main` = code truth.
- Google Sheets `REGISTRO` = operations/finance persistence + formulas.
- AppSheet SD.Live Track = mobile/offline workflow.
- D1 CMS = Draft/Published application content.
- D1 `site_schedule_state` = website-only Site Schedule/Show Day presentation state.
- R2 = managed site media.
- Cloudflare Access = Admin security boundary.
- Finance remains read-only; generic Finance Phase 3 write-back is blocked.
- Calendar controlled create is a separate narrow write authorization.
- Formula-owned Sheet columns remain read-only to Admin.
- COP/USD stay separate.
- Rental pricing remains backend-owned.

## Recent public stabilization

- #123 public accessibility/continuity closeout + Rental integrity guards.
- #125 PA reduction + Misi/Wonderlust mobile Supported Brands + testimonial shrink fix.
- #127 synchronized testimonial expansion.
- #129 natural short-card height + viewport-preserving collapse + stronger PA reduction.
- #131 EN/ES switch preserves expanded Testimonials and scroll position coherently.
- Issue #124 remains the final representative public smoke ledger; do not falsely close it without explicit confirmation.

## Known Admin/CMS required findings

Issue #126:

1. every CMS-managed logo/image control must allow scale up to **250%**, including Testimonials, Trusted By, Supported Brands and every other applicable editor;
2. repeatable CMS cards/collections must be reorderable persistently, preferably explicit drag handle + accessible move up/down controls, preserving stable IDs, Draft → Published and ownership boundaries.

Collect any additional Admin findings in #126 during the audit. Do not implement these one by one before the complete Admin pass unless blocking.

## Finance recent sequence

### #132 — canonical billing date

`Por facturar` now requires canonical `Fecha fin < today`. Today/future/in-progress remains `Flujo bloqueado`; legacy missing end may fall back to `Fecha trabajo`.

### #133 — schema resilience

Finance resolves fields by normalized header names instead of relying on fixed physical positions.

### #134 — payment dates + LiventX review

- corrected ambiguous M/D date behavior that caused false negative payment durations;
- added `LiventX · Listo para firmar` queue;
- eligibility = LiventX + evaluation present + signature missing + not paid + beyond `Pendiente Envio`;
- monthly signing review emphasized from day 20 through month end.

### #135 — LiventX portal

Direct CTA to `https://proveedores.aoscentral.com` from the ready-to-sign card and modal.

### #136 — future Finance Document Generator

Roadmap only: Cuenta de cobro / Cotización / Factura or invoice draft, branded PDF/revisions and reuse of existing operational data. Never represent a local PDF as DIAN-valid electronic invoicing until compliant architecture/provider integration is designed.

### #137 — Finance connection hotfix

Production was observed stuck at `Connecting to SD.Live Track…` after the date-transport changes.

Merged fix:

- dedicated Finance transport guard;
- Google Sheets formatted-date transport (previously production-proven);
- recognized Finance date columns normalized immediately back to numeric Sheets serials;
- deterministic M/D semantics retained;
- 8-second upstream OAuth/Sheets timeout;
- 12-second visible client connection guard;
- outer workspace status mirrors actual source health;
- existing public Contact/Rental rate-limit protections remain delegated and tested.

CI is green. **Production smoke not yet completed at this handoff.**

## Exact immediate next step

ONE manual action only:

Open/reload `https://sdlive.show/admin/finance/`.

Expected:

- it must leave `Connecting to SD.Live Track…`;
- ideal: Finance data/cards load normally;
- acceptable failure behavior: visible timeout/error instead of indefinite loading;
- if loaded, verify existing top cards and LiventX ready-to-sign card are present.

If PASS: record #137 production smoke PASS. Do not ask for a second smoke for #137.

If still broken: treat as blocking runtime regression, inspect current `main`, fix via short branch → tests → PR → CI → squash merge → one smoke. Do not move into normal Admin audit until Finance is usable.

## Then resume Admin visual audit

Record-first sequence, one manual QA action at a time:

1. `/admin/` desktop;
2. `/admin/` mobile;
3. `/admin/finance/` desktop;
4. `/admin/finance/` mobile;
5. `/admin/calendar/` desktop;
6. `/admin/calendar/` mobile;
7. `/admin/calendar/site-schedule/` desktop;
8. `/admin/calendar/site-schedule/` mobile;
9. `/admin/editor/` desktop;
10. `/admin/editor/` mobile.

For every finding:

- add it to issue #126;
- do not branch/fix immediately unless it blocks the audit;
- after the full 10-surface sequence, reconcile against current `main`;
- implement remaining current findings as one coherent Admin stabilization batch;
- run one production smoke for that merged batch.

## Later backlog — do not start yet

- AppSheet reminder/bot alignment with day-after-`Fecha fin` (#83).
- Controlled Calendar edit/workflow actions.
- Calendar Agenda Full Month vs Current + Future filter.
- Show Day simultaneous Primary/Secondary presentation priority.
- Show Day favicon/prepaint polish.
- Finance Document Generator.
- Attio CRM / Dapta.ai evaluations.

## Suggested new-chat instruction

A new ChatGPT conversation should start by reading `PROJECT_STATUS.md`, this checkpoint, `docs/roadmap/post-integration-visual-audit-2026-08-23.md`, issue #126 and current `main`. It should **not** restart old work or assume #137 production PASS without the manual smoke.
