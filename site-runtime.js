/* SD.Live — deprecated compatibility shim.
   UI and navigation no longer depend on Google Tag Manager.
   This file is intentionally inert and can be removed after the GTM loader is deleted. */
(() => {
  window.SDLIVE_SITE_RUNTIME_DEPRECATED = true;
})();
