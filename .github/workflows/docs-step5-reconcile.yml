name: Temporary Step 5 docs reconciler

on:
  push:
    branches:
      - docs/step5-source-map-closeout

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: docs/step5-source-map-closeout
          fetch-depth: 0

      - name: Reconcile Step 5 documentation
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          def replace_once(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected exactly 1 match, found {count}")
              return text.replace(old, new, 1)

          checkpoint = r'''# SD.Live Track — Field / Source-of-Truth Mapping Closeout

**Date:** 2026-08-22 — America/Bogota  
**Control Center:** Step 5 — Finance integration Phase 1  
**Result:** CLOSED / PASS  
**Next gate:** Step 6 — read-only, Admin-only finance insights

## Decision

The current finance architecture remains **Google Sheets + AppSheet** for Phase 2. AppSheet remains the field/offline capture and workflow surface; Google Sheets remains the persistent finance data store and formula owner. The first SD.Live `/admin` integration is **read-only** and must read the underlying Google Sheet/API. No D1 finance copy, write-back, bidirectional sync or parallel financial source of truth is authorized by this closeout.

## Evidence snapshot

The 2026-08-22 `SD.Live Track.xlsx` snapshot and current AppSheet configuration were cross-checked directly.

- 12 workbook tabs audited; `REGISTRO` is the persistent finance table.
- 57 real records and 57 unique persisted IDs at the checkpoint.
- AppSheet exposes 30 columns for `REGISTRO`: `_RowNumber`, 27 physical Sheet columns (`A:AA`) and 2 AppSheet virtual columns.
- 11 physical columns are calculated by Google Sheets.
- `ID` is the AppSheet Key and uses `UNIQUEID()` as Initial Value when a new row is created.
- `Fee Monto` and `Fee %` are AppSheet virtual/App-formula fields and are not persisted in Google Sheets.
- Current Actions, forms and Bots were inspected in AppSheet; the three Bots are notification-only and do not mutate `REGISTRO`.

## Field-level ownership map

Phase 2 keeps the current writer/owner for every field. `/admin` may read approved fields only; it does not become a writer.

| Field / datum | Persistence | Current writer / calculation owner | Phase 2 owner | Direction to future `/admin` | Scope / sensitivity | Notes |
|---|---|---|---|---|---|---|
| `_RowNumber` | AppSheet/system | AppSheet system | AppSheet system | Exclude | Internal technical | Positional helper; never use as durable identity. |
| `Fecha trabajo` | Sheet A | User via AppSheet | Unchanged | Sheet → Worker → Admin | Admin-only operational | Included in `Nuevo Trabajo`; standard Edit remains possible. |
| `Mes` | Sheet B | **Google Sheets formula** | Sheets | Sheet → Worker → Admin | Admin-only finance | Derived from `Fecha trabajo`; never write from Admin. |
| `Año` | Sheet C | **Google Sheets formula** | Sheets | Sheet → Worker → Admin | Admin-only finance | Derived; never write from Admin. |
| `Cliente` | Sheet D | User via AppSheet | Unchanged | Sheet → Worker → Admin | Admin-only business | Included in `Nuevo Trabajo`; exact `LiventX` value participates in workflow conditions. |
| `Proyecto / Show` | Sheet E | User via AppSheet | Unchanged | Sheet → Worker → Admin | Admin-only business | Editable in `Nuevo Trabajo`, standard Edit and `Pago_Form`. |
| `Rol` | Sheet F | User via AppSheet | Unchanged | Sheet → Worker → Admin | Admin-only business | Included in `Nuevo Trabajo`. |
| `Moneda` | Sheet G | User via AppSheet | Unchanged | Sheet → Worker → Admin | Admin-only finance | Editable in `Nuevo Trabajo`, standard Edit and `Pago_Form`; preserve COP/USD separation. |
| `Valor bruto` | Sheet H | User via AppSheet | Unchanged | Sheet → Worker → Admin | Admin-only finance | Editable in `Nuevo Trabajo`, standard Edit and `Pago_Form`. |
| `Impuestos / Fees` | Sheet I | **Google Sheets formula** | Sheets | Sheet → Worker → Admin | Admin-only finance | Derived from persisted payment values/state; Admin must not maintain a parallel copy. |
| `Valor Neto` | Sheet J | **Google Sheets formula** | Sheets | Sheet → Worker → Admin | Admin-only finance | Derived by Sheets; read-only to Admin. |
| `Estado` | Sheet K | User + **AppSheet workflow Actions** | AppSheet workflow / Sheet persistence | Sheet → Worker → Admin | Admin-only finance/workflow | User-selectable at creation/edit; also written by `Enviar Cuenta`, `Evaluar`, `Firmar`, `Set_Pagado`. |
| `Fecha cuenta enviada` | Sheet L | AppSheet `Enviar Cuenta` + manual edit capability | AppSheet workflow / Sheet persistence | Sheet → Worker → Admin | Admin-only finance | `Enviar Cuenta`: sets `Estado="Cuenta enviada"` + `TODAY()` only when `Estado="Pendiente Envio"`. |
| `Fecha evaluación` | Sheet M | AppSheet `Evaluar` + manual edit capability | AppSheet workflow / Sheet persistence | Sheet → Worker → Admin | Admin-only workflow | `Evaluar`: `TODAY()` + `Estado="Evaluada"`; only `Cliente="LiventX"` and blank evaluation date. |
| `Fecha firma` | Sheet N | AppSheet `Firmar` + manual edit capability | AppSheet workflow / Sheet persistence | Sheet → Worker → Admin | Admin-only workflow | `Firmar`: `TODAY()` + `Estado="Firmada"`; only `Cliente="LiventX"` and blank signature date. |
| `Fecha pago` | Sheet O | AppSheet `Set_Pagado` + manual edit capability | AppSheet workflow / Sheet persistence | Sheet → Worker → Admin | Admin-only finance | `Set_Pagado`: `Estado="Pagado"` + `TODAY()` when not already paid and `Fecha cuenta enviada` is present. |
| `Método de pago` | Sheet P | User via AppSheet | Unchanged | Sheet → Worker → Admin | Admin-only finance | Editable in `Nuevo Trabajo`, standard Edit and `Pago_Form`. |
| `Días sin pagar` | Sheet Q | **Google Sheets formula** | Sheets | Sheet → Worker → Admin | Admin-only finance | Aging-derived/current-date field; never write from Admin. |
| `Notas` | Sheet R | User via AppSheet | Unchanged | Sheet → Worker → Admin | Admin-only business | Editable in `Nuevo Trabajo`, standard Edit and `Pago_Form`. |
| `Month Number` | Sheet S | **Google Sheets formula** | Sheets | Sheet → Worker → Admin | Internal/reporting | Derived helper; may be used for ordering, not written by Admin. |
| `Año Pago` | Sheet T | **Google Sheets formula** | Sheets | Sheet → Worker → Admin | Internal/reporting | Derived from `Fecha pago`. |
| `Month Number (pago)` | Sheet U | **Google Sheets formula** | Sheets | Sheet → Worker → Admin | Internal/reporting | Derived helper. |
| `Mes de pago` | Sheet V | **Google Sheets formula** | Sheets | Sheet → Worker → Admin | Admin-only finance | Derived payment-month label. |
| `Rango Aging` | Sheet W | **Google Sheets formula** | Sheets | Sheet → Worker → Admin | Admin-only finance | Derived aging bucket. |
| `ID` | Sheet X | **AppSheet Initial Value `UNIQUEID()`** | AppSheet identity / Sheet persistence | Sheet → Worker internal | Internal identifier | Durable row identity and AppSheet Key; use this instead of `_RowNumber`. Do not display unnecessarily. |
| `Valor Recibido` | Sheet Y | User via AppSheet | Unchanged | Sheet → Worker → Admin | Admin-only finance | Captured in `Pago_Form`; standard Edit remains possible. |
| `MES PAGO KEY` | Sheet Z | **Google Sheets formula** | Sheets | Sheet → Worker internal | Internal/reporting | Derived helper; read only if needed by reporting. |
| `NUM CONTACTO` | Sheet AA | User via AppSheet / standard Edit | Unchanged | Omit from first dashboard unless needed | **Personal contact data** | Not in `Nuevo Trabajo`; keep private and behind Access. |
| `Fee Monto` | Virtual | **AppSheet App formula** | AppSheet; optional Worker recompute for display | No persisted sync | Admin-only finance | Not present in Sheet. If Phase 2 needs it, recompute read-only from authoritative persisted inputs; do not create D1 ownership. |
| `Fee %` | Virtual | **AppSheet App formula** | AppSheet; optional Worker recompute for display | No persisted sync | Admin-only finance | Same rule as `Fee Monto`; no persisted duplicate solely for dashboard convenience. |

## Verified workflow ownership

### Record creation

`Nuevo Trabajo` is an AppSheet Form over `REGISTRO`. It writes these user fields in the current manual column order:

1. `Fecha trabajo`
2. `Cliente`
3. `Proyecto / Show`
4. `Rol`
5. `Moneda`
6. `Valor bruto`
7. `Estado`
8. `Método de pago`
9. `Notas`

Saving creates the row, AppSheet assigns `ID=UNIQUEID()`, and Sheets owns the downstream physical formulas. `Form Saved` uses the normal Go Back behavior; it does not run a separate data Action.

### Billing / collection Actions

- `Enviar Cuenta` → set `Estado="Cuenta enviada"` and `Fecha cuenta enviada=TODAY()`; only when `[Estado]="Pendiente Envio"`; confirmation enabled.
- `Evaluar` → set `Fecha evaluación=TODAY()` and `Estado="Evaluada"`; only for exact `Cliente="LiventX"` with blank evaluation date; confirmation enabled.
- `Firmar` → set `Fecha firma=TODAY()` and `Estado="Firmada"`; only for exact `Cliente="LiventX"` with blank signature date; confirmation enabled.
- The individual `Firmar` Action does not itself require `Fecha evaluación`; the separate audited collection rules are responsible for preventing LiventX from entering collection before both evaluation and signature are complete.

### Payment workflow

`Pagado+Valor` is a grouped Action and runs in this order:

1. `Set_Pagado`
2. `Go to Pago_Form`

`Pagado+Valor` is available only when `NOT(IN([Estado], {"Pagado", "Pendiente Envio"}))` and asks for confirmation.

`Set_Pagado` writes `Estado="Pagado"` and `Fecha pago=TODAY()` when `[Estado]<>"Pagado"` and `Fecha cuenta enviada` is not blank. `Go to Pago_Form` then opens the same row by durable `ID` with `LINKTOROW([ID], "Pago_Form")`.

`Pago_Form` is a hidden auxiliary Form over the existing `REGISTRO` row. Its manual field list is `Valor bruto`, `Proyecto / Show`, `Valor Recibido`, `Moneda`, `Método de pago`, `Notas`; saving performs normal Go Back and no additional business Action.

**Known workflow characteristic:** the row is marked `Pagado` before `Pago_Form` is saved. This is documented behavior, not changed by Step 5. Any future write-back design must either preserve it deliberately or redesign it explicitly with migration/rollback evidence.

### General edit path

`Edit_Job` is navigation only: `LINKTOROW([ID], "Todos")`. `Todos` is a hidden Deck over `REGISTRO`; row selection opens the row detail and the standard system `Edit` / `Delete` actions remain available. Phase 2 `/admin` must not duplicate those writes/deletes.

## Bots / automation ownership

The three current AppSheet Bots were inspected:

- `Enviar nueva cuenta de cobro`
- `Cobro 7 dias+`
- `Cobro 14-30 dias`

All three execute **Notification** steps only. No Bot performs `set values`, row add/delete, data Action or other mutation of `REGISTRO`.

**Future backlog, not active work:** the owner reports the current AppSheet push reminders have not been received in practice. Before changing finance logic, diagnose push delivery/recipient configuration. A later notification-delivery improvement may send the same existing reminder rules through **email and WhatsApp** as additional channels. Those channels must remain notification-only, reuse the existing finance conditions rather than fork business logic, and must not become a second financial source of truth.

## Phase 2 read contract

The first `/admin` implementation may read the Sheet/API and derive presentation-only aggregates such as:

- pending invoices / accounts receivable;
- COP and USD split;
- aging / collection priority;
- deductions / fees where supported by authoritative inputs;
- relevant jobs/events from the mapped finance data.

Rules:

- `/admin` only, behind existing Cloudflare Access;
- no public finance endpoint;
- no mutation of Google Sheets/AppSheet;
- no D1 finance mirror;
- no bidirectional sync;
- do not use `_RowNumber` as identity;
- do not persist AppSheet virtual fields merely to display them;
- if a display metric must be recomputed in the Worker, it must be a read-only derivation from mapped authoritative fields and covered by tests.

## Gate transition

- Step 5 — field/source-of-truth mapping: **CLOSED / PASS**.
- Step 6 — read-only, Admin-only finance insights: **F / ACTIVE GATE**.
- Availability/WhatsApp remains the explicit eligible parallel track; eligibility does not make it active automatically.
- Finance write-back, D1 finance ownership and bidirectional sync remain unauthorized until later phases explicitly satisfy their prerequisites.
'''

          # --- Control Center roadmap ---
          p = Path('docs/roadmap/sdlive-control-center.md')
          text = p.read_text()
          text = replace_once(
              text,
              '**Status:** Active sequenced initiative — Steps 1–4 are closed; **Step 5 field/source-of-truth mapping is the current F Active Gate**. The retained finance system is now branded **SD.Live Track**. Availability/WhatsApp is eligible as the documented parallel track but is not active automatically.',
              '**Status:** Active sequenced initiative — Steps 1–5 are closed; **Step 6 read-only, Admin-only finance insights is the current F Active Gate**. The retained finance system is branded **SD.Live Track** and its field-level ownership map is closed. Availability/WhatsApp remains eligible as the documented parallel track but is not active automatically.',
              'control-center status'
          )
          pattern = re.compile(r'### 5\. Finance integration Phase 1 — source-of-truth mapping — 🚧 F ACTIVE GATE\n.*?(?=\n### 6\. Finance integration Phase 2 — read-only, Admin-only insights)', re.S)
          replacement = '''### 5. Finance integration Phase 1 — source-of-truth mapping — ✅ CLOSED

**CLOSED / PASS on 2026-08-22.** Evidence: `docs/checkpoints/sdlive-track-source-of-truth-2026-08-22.md`.

The real `SD.Live Track.xlsx` backing workbook and current AppSheet configuration were cross-checked field-by-field. The mapping now distinguishes persisted user/AppSheet fields, Google-Sheets-calculated fields, AppSheet-generated identity, AppSheet virtual fields, workflow Actions and notification-only Bots.

Key decisions:

- [x] `REGISTRO` remains the persistent finance table in Google Sheets for Phase 2.
- [x] AppSheet remains the offline field-capture and workflow surface.
- [x] `ID` is the durable AppSheet-generated Key (`UNIQUEID()`); `_RowNumber` is not an integration identity.
- [x] The 11 Sheets-calculated physical columns remain owned by Sheets and read-only to future Admin integration.
- [x] `Fee Monto` / `Fee %` are AppSheet virtual fields; a future Worker may recompute them read-only if needed, but must not persist a parallel owner merely for display.
- [x] `Estado`, billing/evaluation/signature/payment dates and payment capture workflows were traced through the current Actions/forms.
- [x] All three Bots were confirmed notification-only; none mutates `REGISTRO`.
- [x] Phase 2 read scope and privacy boundaries are documented.

No D1 finance copy, write-back, bidirectional sync or parallel financial ownership was introduced.
'''
          text, n = pattern.subn(replacement, text, count=1)
          if n != 1:
              raise SystemExit(f'control-center Step 5 block: expected 1 match, found {n}')
          text = replace_once(
              text,
              '### 6. Finance integration Phase 2 — read-only, Admin-only insights',
              '### 6. Finance integration Phase 2 — read-only, Admin-only insights — 🚧 F ACTIVE GATE',
              'control-center Step 6 header'
          )
          notification_block = '''#### Finance reminder delivery hardening — ⏳ D FUTURE BACKLOG

The current AppSheet finance Bots are notification-only and do not write data. The owner reports the push reminders have not been received in practice. Preserve this as a separate delivery/reliability backlog item rather than changing collection logic during the read-only integration.

- [ ] Diagnose current AppSheet push notification delivery, recipients and runtime history.
- [ ] Add email delivery for the same approved reminder conditions if useful.
- [ ] Add WhatsApp delivery for the same approved reminder conditions if useful.
- [ ] Keep these channels notification-only; reuse one business-rule source and do not fork finance state/ownership.

'''
          text = replace_once(text, '\n### 7. Availability-aware contact widget + WhatsApp AI qualification', '\n' + notification_block + '### 7. Availability-aware contact widget + WhatsApp AI qualification', 'control-center notification backlog')
          text = replace_once(text, '- Steps 1–4 are prerequisites for the finance/control-center integration track and are now closed.', '- Steps 1–5 are prerequisites for the finance/control-center integration track and are now closed; Step 6 is the current read-only integration gate.', 'control-center prerequisites')
          text = replace_once(
              text,
              'This **14.5 Control Center** initiative owns their sequencing and reprioritization. Step 4 rename is closed with **SD.Live Track** applied and mobile/offline smoke passed; **Step 5 field/source-of-truth mapping is the current active gate**.',
              'This **14.5 Control Center** initiative owns their sequencing and reprioritization. Steps 4–5 are closed with **SD.Live Track** applied, mobile/offline smoke passed and the field/source-of-truth map documented; **Step 6 read-only Admin finance insights is the current active gate**.',
              'control-center relationship close'
          )
          p.write_text(text)

          # --- Root README ---
          p = Path('README.md')
          text = p.read_text()
          text = replace_once(
              text,
              '| Admin access | Cloudflare Access |\n| Future CRM / Lead→Quote→Project→Invoice ownership | **TBD until explicitly designed; do not duplicate sources** |',
              '| Admin access | Cloudflare Access |\n| Current finance persistence / workflow | Google Sheets `REGISTRO` + AppSheet; field map closed in `docs/checkpoints/sdlive-track-source-of-truth-2026-08-22.md` |\n| Finance `/admin` Phase 2 | Read-only Worker view over the underlying Google Sheet/API; no D1 finance mirror or write-back |\n| Future CRM / Lead→Quote→Project→Invoice ownership | **TBD until explicitly designed; do not duplicate sources** |',
              'README source-of-truth finance rows'
          )
          text = replace_once(
              text,
              '- **SD.Live as Control Center — active sequenced initiative:** P3.4, Security and the full finance audit are closed. The finance decision is **repair + integrate, not rewrite**; AppSheet keeps offline field capture. Current gate is the brand-coherent rename, followed by field/source mapping and then read-only `/admin` insights. Full evidence: `docs/audits/nextpay26-repair-vs-rewrite-2026-08-22.md`; sequence: `docs/roadmap/sdlive-control-center.md`.',
              '- **SD.Live as Control Center — active sequenced initiative:** P3.4, Security, finance audit, SD.Live Track rename and field/source-of-truth mapping are closed. The finance decision remains **repair + integrate, not rewrite**; AppSheet keeps offline field capture. **Current F gate is Step 6: read-only `/admin` finance insights** over the underlying Google Sheet/API. Evidence: `docs/checkpoints/sdlive-track-source-of-truth-2026-08-22.md`; sequence: `docs/roadmap/sdlive-control-center.md`.',
              'README Control Center active bullet'
          )
          text = replace_once(
              text,
              '- Existing Google Sheets + AppSheet finance system is retained after audit; AppSheet remains the offline capture surface. SD.Live integration still waits for rename + per-field source-of-truth mapping.',
              '- Existing Google Sheets + AppSheet finance system is retained after audit; AppSheet remains the offline capture/workflow surface and Google Sheets `REGISTRO` remains Phase 2 persistence. Field ownership is mapped; the next integration is read-only `/admin`, with no D1 finance mirror/write-back.',
              'README finance retained bullet'
          )
          anchor = '- Automatic Show Day from Calendar/AppSheet with configurable window and manual override.'
          text = replace_once(
              text,
              anchor,
              '- **Finance reminder delivery — future backlog:** current AppSheet Bots are notification-only; diagnose why owner push reminders have not been received, then optionally add email and WhatsApp delivery using the same approved reminder rules. Do not create finance writes or duplicate collection logic in the notification layer.\n' + anchor,
              'README finance notification backlog'
          )
          p.write_text(text)

          # --- PROJECT_STATUS ---
          p = Path('PROJECT_STATUS.md')
          text = p.read_text()
          replacements = [
              ('| Trabajo activo | **Control Center Step 4 — brand-coherent rename** |', '| Trabajo activo | **Control Center Step 6 — read-only Admin finance insights** |', 'PROJECT_STATUS active work'),
              ('| Milestone actual | SD.Live as Control Center — preserve repaired finance system, rename before integration |', '| Milestone actual | SD.Live as Control Center — field ownership closed; begin read-only finance integration |', 'PROJECT_STATUS milestone'),
              ('| Estado | **P3.0–P3.4 CERRADOS; Security CERRADO; Finance audit CERRADO; Rename promovido a F** |', '| Estado | **P3.0–P3.4 CERRADOS; Security CERRADO; Finance audit CERRADO; Rename CERRADO; Source mapping CERRADO** |', 'PROJECT_STATUS state'),
              ('| Active Gate | **F — Control Center Step 4: brand-coherent rename** |', '| Active Gate | **F — Control Center Step 6: read-only, Admin-only finance insights** |', 'PROJECT_STATUS active gate'),
              ('| Gate posterior | **Field/source-of-truth mapping → read-only Admin insights; Availability/WhatsApp remains eligible as the explicit parallel track** |', '| Gate posterior | **After read-only validation: later write-back phase; Availability/WhatsApp remains eligible as the explicit parallel track** |', 'PROJECT_STATUS next gate'),
              ('| Finance system actual | Google Sheets + AppSheet, repair path retained | audit 2026-08-22: AppSheet preserves offline capture; field-level ownership mapping remains mandatory before SD.Live integration |', '| Finance system actual | Google Sheets `REGISTRO` + AppSheet, repair path retained | field/source mapping CLOSED 2026-08-22; AppSheet owns offline capture/workflows, Sheets owns persistence/formulas; checkpoint `docs/checkpoints/sdlive-track-source-of-truth-2026-08-22.md` |', 'PROJECT_STATUS finance source row'),
              ('| SD.Live as Control Center | Steps 1–3 closed; repair + integrate decision recorded; Step 4 rename active | **F — Step 4 Active Gate** | `docs/audits/nextpay26-repair-vs-rewrite-2026-08-22.md` + `docs/roadmap/sdlive-control-center.md`; preserve Sheets/AppSheet logic/offline capture → rename → source mapping → read-only Admin insights |', '| SD.Live as Control Center | Steps 1–5 closed; repair + integrate, rename and field mapping recorded; Step 6 read-only Admin insights active | **F — Step 6 Active Gate** | `docs/checkpoints/sdlive-track-source-of-truth-2026-08-22.md` + `docs/roadmap/sdlive-control-center.md`; Sheets/AppSheet ownership preserved → read-only Admin insights next |', 'PROJECT_STATUS evidence Control Center row'),
          ]
          for old, new, label in replacements:
              text = replace_once(text, old, new, label)
          control_row = '| SD.Live as Control Center | Steps 1–5 closed; repair + integrate, rename and field mapping recorded; Step 6 read-only Admin insights active | **F — Step 6 Active Gate** | `docs/checkpoints/sdlive-track-source-of-truth-2026-08-22.md` + `docs/roadmap/sdlive-control-center.md`; Sheets/AppSheet ownership preserved → read-only Admin insights next |'
          notification_row = '| SD.Live Track reminder delivery | Current 3 AppSheet Bots verified notification-only; owner reports push reminders not received | D — future delivery hardening | Diagnose AppSheet push delivery; optional email + WhatsApp using same reminder conditions; no finance data writes/duplicate state logic |'
          text = replace_once(text, control_row, control_row + '\n' + notification_row, 'PROJECT_STATUS notification row')
          p.write_text(text)

          # --- Master roadmap ---
          p = Path('ROADMAP_MASTER_CHECKLIST.md')
          text = p.read_text()
          text = replace_once(
              text,
              '🚧 **F — Control Center Step 4: brand-coherent rename.** P3.4, Security and the full finance audit are closed. The audit decision is **repair + integrate; no rewrite**, preserving AppSheet offline capture. Rename now precedes field/source mapping and read-only `/admin` insights. Availability/WhatsApp remains eligible as the explicit parallel track but is not active automatically.',
              '🚧 **F — Control Center Step 6: read-only, Admin-only finance insights.** P3.4, Security, the finance audit, SD.Live Track rename and field/source-of-truth mapping are closed. The decision remains **repair + integrate; no rewrite**, preserving AppSheet offline capture and Google Sheets persistence/formulas. Availability/WhatsApp remains eligible as the explicit parallel track but is not active automatically.',
              'master current active gate'
          )
          text = replace_once(
              text,
              '**Status: 🚧 F — Step 4 brand-coherent rename is active; Steps 1–3 are closed.** Audit evidence: `docs/audits/nextpay26-repair-vs-rewrite-2026-08-22.md`. Decision: repair + integrate, preserve AppSheet offline capture. Required order now is rename → field/source mapping → read-only Admin insights → later write-back/bidirectional phases. Availability/WhatsApp remains the sole explicit parallel-track exception and is eligible, not active automatically.',
              '**Status: 🚧 F — Step 6 read-only, Admin-only finance insights is active; Steps 1–5 are closed.** Audit evidence: `docs/audits/nextpay26-repair-vs-rewrite-2026-08-22.md`; field map evidence: `docs/checkpoints/sdlive-track-source-of-truth-2026-08-22.md`. Decision: repair + integrate, preserve AppSheet offline capture and Sheets ownership. Required order now is read-only Admin insights → validated later write-back/bidirectional phases. Availability/WhatsApp remains the sole explicit parallel-track exception and is eligible, not active automatically.',
              'master 14.5 status'
          )
          text = replace_once(text, '- ⏳ Rename NextPay26 before deeper integration.', '- ✅ Renamed the surviving user-facing system to **SD.Live Track**; technical/internal AppSheet identity intentionally preserved where needed.', 'master rename item')
          text = replace_once(text, '- ⏳ Define source of truth per field before any sync/read model.', '- ✅ Field/source-of-truth mapping closed; see `docs/checkpoints/sdlive-track-source-of-truth-2026-08-22.md`.', 'master source map item')
          text = replace_once(text, '- ⏳ First SD.Live integration is read-only `/admin` insight via underlying Google Sheet/API, not an assumed AppSheet API layer.', '- 🚧 **F active:** First SD.Live integration is read-only `/admin` insight via underlying Google Sheet/API, not an assumed AppSheet API layer.', 'master Step6 item')
          text = replace_once(
              text,
              '- ⏳ Avoid unnecessary duplication or second financial source of truth.',
              '- ⏳ Avoid unnecessary duplication or second financial source of truth.\n- ⏳ **Finance reminder delivery hardening:** diagnose why current AppSheet push reminders have not been received; optionally add email + WhatsApp delivery using the same approved reminder conditions. Notification channels remain read-only and must not fork finance logic.',
              'master notification backlog'
          )
          p.write_text(text)

          # --- Checkpoint file ---
          cp = Path('docs/checkpoints/sdlive-track-source-of-truth-2026-08-22.md')
          if cp.exists():
              raise SystemExit('checkpoint already exists unexpectedly')
          cp.write_text(checkpoint)

          # Sanity checks: stale active-gate wording should be gone from the primary docs.
          checks = {
              'README.md': ['Current gate is the brand-coherent rename', 'waits for rename + per-field source-of-truth mapping'],
              'PROJECT_STATUS.md': ['**F — Control Center Step 4: brand-coherent rename**'],
              'ROADMAP_MASTER_CHECKLIST.md': ['**F — Control Center Step 4: brand-coherent rename.**', '**Status: 🚧 F — Step 4 brand-coherent rename is active; Steps 1–3 are closed.**'],
              'docs/roadmap/sdlive-control-center.md': ['**Step 5 field/source-of-truth mapping is the current F Active Gate**'],
          }
          for filename, stale in checks.items():
              body = Path(filename).read_text()
              for phrase in stale:
                  if phrase in body:
                      raise SystemExit(f'stale active-gate phrase remains in {filename}: {phrase}')

          print('Step 5 documentation reconciliation completed.')
          PY

          git rm .github/workflows/docs-step5-reconcile.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md PROJECT_STATUS.md ROADMAP_MASTER_CHECKLIST.md docs/roadmap/sdlive-control-center.md docs/checkpoints/sdlive-track-source-of-truth-2026-08-22.md
          git commit -m "Docs: close finance source mapping and activate read-only insights"
          git push origin HEAD:docs/step5-source-map-closeout
